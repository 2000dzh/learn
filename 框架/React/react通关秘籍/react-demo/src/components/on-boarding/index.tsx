interface MaskProps {
  element: HTMLElement

  container?: HTMLElement

  renderMaskContent?: (wrapper: React.ReactNode) => React.ReactNode
}

export const Mask: React.FC = (props) => {
  const {} = props
}
